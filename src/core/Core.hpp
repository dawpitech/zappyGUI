#include <iostream>
#include "Map.hpp"
namespace GUI {
    class Core {
        public:
        private:
            /* Map _map; */
            /* Layer _layer; */
            std::size_t _timeUnit;
            std::size_t _time;
    };
} // namespace GUI
